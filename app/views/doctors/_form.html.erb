<%= form_for(@doctor) do |f| %>
  <% if @doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
      <% @doctor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :hour_from %><br />
    <%= f.time_select :hour_from %>
  </div>
  <div class="field">
    <%= f.label :hour_to %><br />
    <%= f.time_select :hour_to %>
  </div>
  <div class="field">
    <%= f.label :shift_duration %><br />
    <%= f.time_select :shift_duration %>
  </div>
  <div class="field">
    <%= f.label :medical_specification %><br />
    <%= f.text_field :medical_specification %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
