<%= form_for(@medical_specification) do |f| %>
  <% if @medical_specification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_specification.errors.count, "error") %> prohibited this medical_specification from being saved:</h2>

      <ul>
      <% @medical_specification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
